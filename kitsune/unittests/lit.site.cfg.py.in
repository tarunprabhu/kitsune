@LIT_SITE_CFG_IN_HEADER@

config.kitsune_source_dir = path(r"@KITSUNE_SOURCE_DIR@")
config.kitsune_binary_dir = path(r"@KITSUNE_BINARY_DIR@")

# Load the main configuration file.
lit_config.load_config(
    config, os.path.join(config.kitsune_source_dir, "unittests", "lit.cfg.py"))
