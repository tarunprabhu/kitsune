# Compiler and/or linker options can be added here to add to (or override) those
# added automatically by Kitsune for the 'openmp' target. 
#
# ${KITSUNE_OPENMP_TARGET_LINK_FLAGS}
# -lkitrt ${KITSUNE_OPENMP_TARGET_LINK_LIBS}
#
# Other options can be added here as well, but any options not exclusively
# intended for the 'openmp' target should be added to ${KITSUNE_C_FRONTEND}.cfg or ${KITSUNE_CXX_FRONTEND}.cfg.

