# Build System

TODO: Write documentation describing how Kitsune-specific changes have been
made to LLVM's build system.

This should also include descriptions of how Kitsune builds the dependencies
that are required (currently, only for the tapir targets, but the runtime
systems may have other dependencies).
