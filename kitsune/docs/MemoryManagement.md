# Memory Management

TODO: Describe Kitsune's memory allocation and deallocation functions. These
are required to transparently support both CPU and GPU tapir targets since the
latter requires dynamic memory allocations to be performed in unified memory
(UVM).
