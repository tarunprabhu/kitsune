# LLVM IR Attributes

TODO: Write documentation describing the design and use of Kitsune-specific
"attributes". These may be attached to several LLVM-IR entities such as
`Argument`s, `Function`s, `GlobalVariables`s, `Instruction`s, `Loop`s, and
`Module`s.
