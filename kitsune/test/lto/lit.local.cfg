# For the multi-file tests, the source files are in input/. But the source files
# in this directory are valid test files. The .test files contain multi-step
# tests.
config.suffixes = ['.c', '.cpp', '.f', '.f90', '.test']
