@LIT_SITE_CFG_IN_HEADER@

import sys

config.llvm_build_mode = lit_config.substitute("@LLVM_BUILD_MODE@")
config.kitsune_obj_root = path(r"@KITSUNE_BINARY_DIR@")
config.kitsune_src_root = path(r"@KITSUNE_SOURCE_DIR@")

config.kitrt_obj_root = os.path.join(config.kitsune_obj_root, "runtime")
config.kitrt_src_root = os.path.join(config.kitsune_src_root, "runtime")

# Let the main config do the real work.
lit_config.load_config(
    config, os.path.join(config.kitsune_src_root, "test", "rt-unit", "lit.cfg.py"))
