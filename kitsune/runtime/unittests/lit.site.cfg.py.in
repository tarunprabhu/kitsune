@LIT_SITE_CFG_IN_HEADER@

config.kitrt_binary_dir = path(r"@KITRT_BINARY_DIR@")
config.kitrt_source_dir = path(r"@KITRT_SOURCE_DIR@")

# Load the main configuration file.
lit_config.load_config(
    config, os.path.join(config.kitrt_source_dir, "unittests", "lit.cfg.py"))
