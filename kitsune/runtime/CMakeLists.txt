#
# Copyright (c) 2021 Triad National Security, LLC. All rights reserved.
#
# This file is part of the kitsune project. It is released under the LLVM
# license.
#

include (CheckLinkerFlag)

check_linker_flag(CXX "LINKER:-z,now" LINKER_SUPPORTS_Z_NOW)

set(KITRT_OBJLIB "kitrt_objlib")

set(KITSUNE_CPU_TERMCOLOR "nullptr")
set(KITSUNE_CUDA_TERMCOLOR "nullptr")
set(KITSUNE_HIP_TERMCOLOR "nullptr")
if (KITSUNE_TERMCOLOR_ENABLED)
  set(KITSUNE_CPU_TERMCOLOR "ANSI_CYAN")
  set(KITSUNE_CUDA_TERMCOLOR "ANSI_BLUE")
  set(KITSUNE_HIP_TERMCOLOR "ANSI_YELLOW")
endif ()

add_library(${KITRT_OBJLIB} OBJECT
  kitrt.cpp
  memory.cpp
  memory_map.cpp
  common/env.cpp
  common/instrutils.cpp
  common/logging.cpp
  common/utils.cpp
  global/global.cpp
  timer/timer.cpp
)

add_dependencies(${KITRT_OBJLIB} ${KITRT_DEPENDS})

# Here, ${LLVM_MAIN_INCLUDE_DIR} points to llvm/include in the source directory.
# ${LLVM_INCLUDE_DIR} points to include/ in the directory where cmake was run
# when building all of Kitsune (not just the runtime). This is where the files
# generated by LLVM's tablegen are added. clang's tablegen adds files elsewhere
# but we don't need to use those directly in any of our runtimes.
# ${KITSUNE_SOURCE_DIR} points to kitsune/ in the top-level source directory.
# This is the same as ${CMAKE_CURRENT_SOURCE_DIR}/../include.
# ${KITSUNE_BINARY_DIR} is the subdirectory of the top-level build directory
# where Kitsune is built.
target_include_directories(${KITRT_OBJLIB} PUBLIC
  ${LLVM_MAIN_INCLUDE_DIR}
  ${LLVM_INCLUDE_DIR}
  ${KITSUNE_SOURCE_DIR}/include
  ${KITSUNE_BINARY_DIR}/include
  ${CMAKE_CURRENT_SOURCE_DIR})

target_link_libraries(${KITRT_OBJLIB} PUBLIC
  ${KITSUNE_COMMON_LIBS})

if (LLVM_ENABLE_ASSERTIONS)
  # TODO: Is it always safe to use -UNDEBUG here? The odds are that this will be
  # compiled with GCC or Clang, so it probably is, but is there a better way to
  # do this?
  target_compile_options(${KITRT_OBJLIB} PUBLIC
    -UNDEBUG)
endif()

if (KITSUNE_COLORS_ENABLED)
  target_compile_definitions(${KITRT_OBJLIB} PUBLIC
    KITRT_COLORS_ENABLED)

  target_include_directories(${KITRT_OBJLIB} SYSTEM PUBLIC
    ${KITSUNE_CURSES_INCDIRS})

  target_link_libraries(${KITRT_OBJLIB} PUBLIC
    ${KITSUNE_CURSES_LIBS})
endif ()

if (KITSUNE_PAPI_ENABLED)
  target_sources(${KITRT_OBJLIB} PUBLIC
    papi/kitpapi.cpp)

  target_compile_definitions(${KITRT_OBJLIB} PUBLIC
    KITRT_PAPI_ENABLED)

  target_include_directories(${KITRT_OBJLIB} SYSTEM PUBLIC
    ${KITSUNE_PAPI_INCDIRS})

  target_link_libraries(${KITRT_OBJLIB} PUBLIC
    ${KITSUNE_PAPI_LIBS})
endif ()

if (KITSUNE_CUDA_ENABLED)
  set (SOURCES
    cuda/kitcuda.cpp
    cuda/dylib_support.cpp
    cuda/launching.cpp
    cuda/memory.cpp
    cuda/streams.cpp
  )

  set_source_files_properties(${SOURCES} PROPERTIES
    COMPILE_DEFINITIONS
    "KITRT_LOG_TAG=\"cuda\";KITRT_LOG_COLOR=${KITSUNE_CUDA_TERMCOLOR}")

  target_sources(${KITRT_OBJLIB} PUBLIC ${SOURCES})

  target_compile_definitions(${KITRT_OBJLIB} PUBLIC
    KITRT_CUDA_ENABLED)

  target_include_directories(${KITRT_OBJLIB} SYSTEM PUBLIC
    ${KITSUNE_CUDA_INCDIRS})

  target_link_libraries(${KITRT_OBJLIB} PUBLIC
    ${KITSUNE_CUDA_LIBS})
endif()

if (KITSUNE_HIP_ENABLED)
  set(SOURCES
    hip/kithip.cpp
    hip/dylib_support.cpp
    hip/launching.cpp
    hip/memory.cpp
    hip/streams.cpp
  )

  set_source_files_properties(${SOURCES} PROPERTIES
    COMPILE_DEFINITIONS
    "KITRT_LOG_TAG=\"hip\";KITRT_LOG_COLOR=${KITSUNE_HIP_TERMCOLOR}")

  target_sources(${KITRT_OBJLIB} PUBLIC ${SOURCES})

  target_compile_definitions(${KITRT_OBJLIB} PUBLIC
    # FIXME: It looks like __HIP_PLATFORM_AMD__ is defined in the kitsune
    # runtime headers for hip. This is probably unnecessary. Alternatively, we
    # could remove it from those headers and only include it here if is required
    # to be defined before any hip headers are included.
    __HIP_PLATFORM_AMD__
    KITRT_HIP_ENABLED)

  target_include_directories(${KITRT_OBJLIB} SYSTEM PUBLIC
    ${KITSUNE_HIP_INCDIRS})

  target_link_libraries(${KITRT_OBJLIB} PUBLIC
    ${KITSUNE_HIP_LIBS})
endif()

if (KITSUNE_OPENCILK_ENABLED)
  set(SOURCES
    opencilk/kitocilk.cpp
  )

  set_source_files_properties(${SOURCES} PROPERTIES
    COMPILE_DEFINITIONS
    "KITRT_LOG_TAG=\"opencilk\";KITRT_LOG_COLOR=${KITSUNE_CPU_TERMCOLOR}")

  target_sources(${KITRT_OBJLIB} PUBLIC ${SOURCES})

  target_compile_definitions(${KITRT_OBJLIB} PUBLIC
    KITRT_OPENCILK_ENABLED)

  target_link_libraries(${KITRT_OBJLIB} PUBLIC
    ${KITSUNE_OPENCILK_LIBS})
endif ()

if (KITSUNE_OPENMP_ENABLED)
  set(SOURCES
    openmp/kitomp.cpp
  )

  set_source_files_properties(${SOURCES} PROPERTIES
    COMPILE_DEFINITIONS
    "KITRT_LOG_TAG=\"openmp\";KITRT_LOG_COLOR=${KITSUNE_CPU_TERMCOLOR}")

  target_sources(${KITRT_OBJLIB} PUBLIC ${SOURCES})

  target_compile_definitions(${KITRT_OBJLIB} PUBLIC
    KITRT_OPENMP_ENABLED)

  target_include_directories(${KITRT_OBJLIB} SYSTEM PUBLIC
    ${KITSUNE_OPENMP_INCDIRS})

  target_link_libraries(${KITRT_OBJLIB} PUBLIC
    ${KITSUNE_OPENMP_LIBS})
endif ()

if (KITSUNE_PTHREADS_ENABLED)
  set(SOURCES
    pthreads/kitpthr.cpp
  )

  set_source_files_properties(${SOURCES} PROPERTIES
    COMPILE_DEFINITIONS
    "KITRT_LOG_TAG=\"pthreads\";KITRT_LOG_COLOR=${KITSUNE_CPU_TERMCOLOR}")

  target_sources(${KITRT_OBJLIB} PUBLIC ${SOURCES})

  target_compile_definitions(${KITRT_OBJLIB} PUBLIC
    KITRT_PTHREADS_ENABLED)
endif ()

if (KITSUNE_QTHREADS_ENABLED)
  set(SOURCES
    qthreads/kitqthr.cpp
  )

  set_source_files_properties(${SOURCES} PROPERTIES
    COMPILE_DEFINITIONS
    "KITRT_LOG_TAG=\"qthreads\";KITRT_LOG_COLOR=${KITSUNE_CPU_TERMCOLOR}")

  target_sources(${KITRT_OBJLIB} PUBLIC ${SOURCES})

  target_compile_definitions(${KITRT_OBJLIB} PUBLIC
    KITRT_QTHREADS_ENABLED)

  target_include_directories(${KITRT_OBJLIB} SYSTEM PUBLIC
    ${KITSUNE_QTHREADS_INCDIRS})

  target_link_libraries(${KITRT_OBJLIB} PUBLIC
    ${KITSUNE_QTHREADS_LIBS})
endif ()

add_library(${KITRT_DSO} SHARED
  $<TARGET_OBJECTS:${KITRT_OBJLIB}>)

# We have added libraries ${KITRT_OBJLIB}. We need all of those for the actual
# Kitsune shared library.
get_target_property(KITRT_LINK_LIBRARIES ${KITRT_OBJLIB} LINK_LIBRARIES)
foreach (LIB IN LISTS KITRT_LINK_LIBRARIES)
  target_link_libraries(${KITRT_DSO} PUBLIC ${LIB})
endforeach()

# We have to set the output directory explicitly, otherwise these will go to the
# main lib/ directory that also contains LLVM. But we actually want this in
# Kitsune's resource directory because all Kitsune-specific headers and
# dependent libraries are intentionally kept "private" to the compiler.
#
# Setting INSTALL_RPATH_USE_LINK_PATH is not always safe since the binary may
# be used on a different system where the required libraries may be (correctly)
# obtained from a different path. We don't really support shipping libraries
# across systems, and generally require that binaries built with Kitsune are
# either run on the same, or a functionally equivalent system, where the
# identical library versions are available at exactly the same locations. We may
# want to consider doing something a bit more robust in the future, but for now,
# this should be ok.
set_target_properties(${KITRT_DSO} PROPERTIES
  LIBRARY_OUTPUT_DIRECTORY ${KITSUNE_RESOURCE_INTDIR}/lib
  INSTALL_RPATH_USE_LINK_PATH TRUE)

if (LINKER_SUPPORTS_Z_NOW)
  target_link_options(${KITRT_DSO} PRIVATE "LINKER:-z,now")
endif ()

# libkitrt is installed alongside the libraries that it depends on such as
# libqthread, libomp etc. in Kitsune's internal resource directory. Ensure that
# these are always picked up, even after installation. The way we have
# configured Cheetah, its libraries will be installed to a directory with the
# name of the default target triple in the same directory.
set(RPATH_RELATIVE_TO_ORIGIN
  "\$ORIGIN/"
  "\$ORIGIN/${LLVM_DEFAULT_TARGET_TRIPLE}/")
set_property(TARGET ${KITRT_DSO} APPEND
  PROPERTY BUILD_RPATH "${RPATH_RELATIVE_TO_ORIGIN}")
set_property(TARGET ${KITRT_DSO} APPEND
  PROPERTY INSTALL_RPATH "${RPATH_RELATIVE_TO_ORIGIN}")

add_library(${KITRT_STATIC} STATIC
  $<TARGET_OBJECTS:${KITRT_OBJLIB}>)

set_target_properties(${KITRT_STATIC} PROPERTIES
  ARCHIVE_OUTPUT_DIRECTORY ${KITSUNE_RESOURCE_INTDIR}/lib)

install(TARGETS ${KITRT_DSO} ${KITRT_STATIC}
  DESTINATION ${KITSUNE_RESOURCE_DIR}/lib/)
